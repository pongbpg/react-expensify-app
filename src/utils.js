console.log('util.js is runing!');

export const square = (x) => x ^ 2;
export const add = (x, y) => x + y;
// const subtract = (x, y) => x - y;
export default (x, y) => x - y;
// export { square, add, subtract }